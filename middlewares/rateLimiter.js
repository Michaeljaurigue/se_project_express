// const rateLimit = require("express-rate-limit");

// const limiter = rateLimit({
//   windowMs: 700 * 1000, // 6 seconds
//   max: 200, // limit each IP to 2 requests per windowMs
// });

// export default limiter;
